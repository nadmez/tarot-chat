{
   "name": "chatbot",
   "version": "0.0.0",
   "type": "module",
   "private": true,
   "scripts": {
      "format": "prettier --write .",
      "prepare": "husky install || true",
      "build:server": "npm run build -w server"
   },
   "devDependencies": {
      "husky": "^9.1.7",
      "lint-staged": "^16.2.7",
      "prettier": "^3.8.0"
   },
   "peerDependencies": {
      "typescript": "^5"
   },
   "workspaces": [
      "packages/*"
   ]
}
